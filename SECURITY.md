# Security policy

请勿在公开 Issue 中提交 API Key、认证文件、私人主题素材或系统日志中的敏感数据。

社区内容必须满足：

- 仅 HTTPS 下载。
- registry URL 指向不可变版本。
- 所有下载文件声明 SHA-256 和字节数。
- 包内只允许声明的 JSON、图片、README 和 LICENSE；禁止脚本、动态库和可执行文件。
- 文件名必须是 basename，禁止绝对路径、`..`、符号链接和目录穿越。
- provider 的 CDP 端口只能监听 `127.0.0.1` 或 `::1`。

发现供应链或本地提权问题时，请先私下联系维护者，再协调披露。
